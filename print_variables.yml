---
- name: Print bigpanda payload
  hosts: localhost
  gather_facts: no

  tasks:
    - name: Resolve Incident
      bigpanda.incident.resolve:
         incident_id: "{{bigpanda.incident}}"
         debug:
           var: incident_id
    
